# eliaspeteri.github.io

To build the app and test it locally, run **npm run build && serve -s build**
The app will then be available by default at **localhost:5000**

To run the development version, simply run **npm start**
