import React from "react";

const Navbar = () => {
    return (
        <nav>
            <a href="#">Home</a>
            <a href="#">About me</a>
            <a href="#">Portfolio</a>
            <a href="#">Contact me</a>
        </nav>
    );
};

export default Navbar;
